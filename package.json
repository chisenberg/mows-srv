{
  "name": "mows-svrnpmt",
  "version": "1.0.0",
  "description": "",
  "author": "",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/chisenberg/mows-svr.git"
  },
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "mkdir -p bin/ && gcc -Wall -pthread -o bin/pulse433 src/pulse433.c -lpigpiod_if2 -lrt",
    "dev-all": "nodemon --watch server.js --watch src/ --watch lib/ --exec 'npm run build && npm run start'",
    "dev": "nodemon --watch server.js --watch src/ --watch lib/ --exec npm run start"
  },
  "dependencies": {
    "dblite": "^0.10.0",
    "express": "^4.16.3"
  },
  "devDependencies": {
    "eslint": "^5.1.0",
    "nodemon": "^1.18.3"
  }
}
