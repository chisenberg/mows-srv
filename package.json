{
  "name": "mows-svr",
  "version": "1.0.0",
  "description": "",
  "author": "",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/chisenberg/mows-svr.git"
  },
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "compile": "mkdir -p bin/ && gcc -Wall -pthread -o bin/pulse433 src/pulse433.c -lpigpiod_if2 -lrt",
    "dev-all": "nodemon --delay 2500ms --watch server.js --watch src/ --watch lib/ --exec 'npm run compile && npm run start'",
    "dev": "nodemon --delay 2500ms --watch server.js --watch src/ --watch lib/ --exec npm run start"
  },
  "dependencies": {
    "async": "^2.6.1",
    "body-parser": "^1.18.3",
    "cors": "^2.8.4",
    "dblite": "^0.10.0",
    "express": "^4.16.3",
    "underscore": "^1.9.1"
  },
  "devDependencies": {
    "eslint": "^5.1.0",
    "nodemon": "^1.18.3"
  }
}
