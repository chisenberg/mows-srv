{
  "name": "mows-srv",
  "version": "1.0.0",
  "description": "",
  "author": "",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/chisenberg/mows-srv.git"
  },
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "build": "mkdir -p bin/ && gcc -Wall -pthread -o bin/pulse433 src/pulse433.c -lpigpiod_if2 -lrt",
    "dev": "nodemon --watch server.js --watch src/* --watch lib/* --exec 'npm run build && npm run start'"
  },
  "dependencies": {
    "express": "^4.16.3"
  },
  "devDependencies": {
    "nodemon": "^1.18.3"
  }
}
